# research-agent-ts

## 概要

Web 検索・自己修正型リサーチエージェント。

LangChain/LangGraph を利用した AI エージェントの開発を学習するために作成したもの。

## 実行方法

1. `npm i` を実行して依存パッケージをインストールする
2. `.env` ファイルを作成する
   - 設定内容は `.env.sample` を参照
3. ターミナルにて `npx ts-node src/index.ts` を実行する
